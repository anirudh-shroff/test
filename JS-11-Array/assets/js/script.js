//*Array : a collection of multiple values of same datatype is an array, this the genera; defintion but it is not applicable to js exclusively
// in js, it is a collection of multiple values, but can be of different datatypes.

let arr = [1, 2, 3, 4, 5];

// console.log(arr.length);

for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
}


